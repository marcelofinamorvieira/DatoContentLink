# Preview integration tests / helpers
# Copy this file to `.env.visual-editing` (or `.env.local`) and fill in the values.
# Files matching `.env*` are ignored by git.

DATOCMS_VISUAL_EDITING_TOKEN=your_preview_api_token
DATOCMS_VISUAL_EDITING_BASE_URL=https://yourproject.admin.datocms.com
# Optional: override the GraphQL preview endpoint
# DATOCMS_VISUAL_EDITING_GRAPHQL_URL=https://graphql.datocms.com/preview
